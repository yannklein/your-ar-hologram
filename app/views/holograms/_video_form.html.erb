<%= simple_form_for @hologram, html: {class: 'new-form-content'} do |f| %>
  <%= f.error_notification %>
  <%= f.input :title, label: 'Choose a great title:'%>
  <%= f.input :description, label: 'Take a breath and go for a cool description:', as: :text, :input_html => {:style => 'width: 100%', :rows => 5 } %>
  <%= f.input :video, label: 'Now upload your video 😎👇'%>
  <%= f.button :submit, value: 'Brew your hologram', class: 'btn btn-primary' %>
<% end %>
